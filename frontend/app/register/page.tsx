'use client';
import Link from 'next/link';import {useState} from 'react';import {api} from '../../lib/api';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{await api('/auth/register',{method:'POST',body:JSON.stringify(form)});location.href='/login'}catch(err){setError(err instanceof Error?err.message:'Unable to register')}}return <main className="auth"><form className="authbox" onSubmit={submit}><div className="eyebrow">Start creating</div><h1>Your studio awaits.</h1><p>Build your first concept with an AI producer in your corner.</p>{(['name','email','password'] as const).map(key=><div className="field" key={key}><label>{key[0].toUpperCase()+key.slice(1)}</label><input type={key==='password'?'password':key==='email'?'email':'text'} required value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></div>)}{error&&<div className="error">{error}</div>}<button className="button lime">Create account</button><p>Already have an account? <Link href="/login">Sign in</Link></p></form></main>}
